"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "'GeorgiaPro-Regular', 'Georgia', 'serif'"
  },
});

export default theme;